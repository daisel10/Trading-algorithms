# ============================================================================
# KAIRÓS Core - Environment Variables
# ============================================================================
# Este archivo contiene SOLO las variables que deben configurarse por entorno.
# La mayoría de la configuración ya está en config/default.toml
#
# Copia este archivo a .env y configura tus valores reales
# ============================================================================

# ----------------------------------------------------------------------------
# 🔑 API Credentials (CRÍTICO - NUNCA commitear estos valores)
# ----------------------------------------------------------------------------
# Estas son las ÚNICAS variables obligatorias en .env
# Usa el formato: KAIROS__EXCHANGE__<NOMBRE>
# El __ (doble guión bajo) separa niveles de jerarquía en la configuración

# OKX Exchange
# Obtén tus keys en: https://www.okx.com/account/my-api
KAIROS__EXCHANGE__OKX_API_KEY=your_okx_api_key_here
KAIROS__EXCHANGE__OKX_API_SECRET=your_okx_api_secret_here
KAIROS__EXCHANGE__OKX_API_PASSPHRASE=your_okx_passphrase_here

# Binance Exchange
# Obtén tus keys en: https://www.binance.com/en/my/settings/api-management
KAIROS__EXCHANGE__BINANCE_API_KEY=your_binance_api_key_here
KAIROS__EXCHANGE__BINANCE_API_SECRET=your_binance_api_secret_here

# ----------------------------------------------------------------------------
# 📝 Variables Opcionales
# ----------------------------------------------------------------------------
# Solo descomenta si necesitas sobrescribir los valores de default.toml

# Cambiar nivel de logs (default: "info,kairos_core=debug")
# RUST_LOG=debug,kairos_core=trace

# Cambiar puerto gRPC (default: 50051)
# KAIROS__GRPC__PORT=50052

# Usar testnet de OKX en lugar de producción
# KAIROS__EXCHANGE__OKX_WS_PUBLIC_URL=wss://wspap.okx.com:8443/ws/v5/public
# KAIROS__EXCHANGE__OKX_WS_PRIVATE_URL=wss://wspap.okx.com:8443/ws/v5/private

# ----------------------------------------------------------------------------
# ⚠️ SEGURIDAD
# ----------------------------------------------------------------------------
# 1. Habilita IP whitelist en tu exchange
# 2. Usa permisos mínimos necesarios para las API keys
# 3. Rota las keys regularmente
# 4. NUNCA compartas o commitees este archivo
# 5. Para producción usa un gestor de secretos (Vault, AWS Secrets Manager, etc.)
